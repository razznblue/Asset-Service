<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/views/style.css">
  <title>SWGU Library</title>
</head>
<body>
  <div class="container">
    <h1>SWGU Library</h1>
    <div class="actions">

      <h2>ACTIONS:</h2>
      <p><a href="<%= baseurl + '/all' %>"><-- VIEW ALL ASSETS --></a></p>
      <p><a href="<%= baseurl + '/drive/download' %>"><-- DOWNLOAD ENTIRE DRIVE --></a></p>
      <p><-- DOWNLOAD FROM DRIVE BY CATEGORY --></p>
      <select name="Category-Download" id="category-download" onchange="downloadCategory()">
        <option selected disabled>Triggers On Change</option>
        <% for (let i = 1; i < categories.length; i++) { %>
          <option value="<%= categories[i] %>"><%= categories[i] %></option>
        <% } %>
      </select>
    </div>

    <form method="POST" action="/upload" enctype="multipart/form-data" id="upload-container">
      <h3>Upload a Single File</h3>
      <select name="Category" id="category">
        <option selected disabled>Category</option>
        <option value="SPRITES">Character Sprites</option>
        <option value="BACKGROUNDS">Backgrounds</option>
        <option value="GAME_TILES">Game Tiles</option>
        <option value="CARD_FRONTS">Toon Cards (front)</option>
        <option value="CARD_BACKS">Toon Cards (back)</option>
        <option value="CARD_HEROS">Toon Hero</option>
        <option value="CARD_REMNANTS">Toon Remnant</option>
        <option value="CARD_OBJECTS">Card Objects</option>
        <option disabled>Json</option>
        <option value="CHARACTERS">Characters</option>
        <option value="GEAR">Gear</option>
        <option value="ABILITIES">Abilities</option>
        <option disabled>Audio(takes too long to upload lol)</option>
        <option disabled value="SFX">SFX</option>
        <option disabled value="LOFI">LOFI</option>
        <option disabled value="HIPHOP">Hip-Hop</option>
      </select>
      <input type="file" name="asset" id="file-upload">
      <input type="submit" id="submit">
    </form>
  </div>



  <script>
    const downloadCategory = () => {
      const category = document.querySelector('#category-download').value;
      if (category) {
        const link = document.createElement('a');
        link.href = `/drive/download?category=${category}`;
        console.log(link.href);
        link.click();
      }
    }
  </script>

</body>
</html>